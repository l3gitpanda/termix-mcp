{
  "name": "termix-mcp",
  "version": "0.6.0",
  "description": "MCP server for Termix: SSH host management, command execution, file operations, Docker and system control, with full audit logging",
  "type": "module",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start:stdio": "node src/index-stdio.mjs",
    "start:http": "node src/index-http.mjs",
    "test": "node --test test/smoke.mjs test/integration.mjs test/access.mjs",
    "verify-live": "node scripts/verify-live.mjs",
    "policy": "node scripts/policy.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "pino": "^10.3.1",
    "zod": "^4.4.3"
  }
}
